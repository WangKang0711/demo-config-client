spring:
  cloud:
      config:
        discovery:
          enabled: true 
          service-id: demo-config-server
        profile: dev
        label: master
        
management:
  security:
     enabled: false